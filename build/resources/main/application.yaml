spring:
  application:
    name: api
  flyway:
    enabled: true
    locations: classpath:db/migrations

api:
  security:
    keys:
      - ${API_KEY_1:default-dev-key-1}
      - ${API_KEY_2:default-dev-key-2}

